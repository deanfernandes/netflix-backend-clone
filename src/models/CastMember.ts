export interface CastMember {
  id: string;
  name: string;
}

export interface FilmCastMember {
  filmId: string;
  castMemberId: string;
}

export interface SeriesCastMember {
  seriesId: string;
  castMemberId: string;
}
